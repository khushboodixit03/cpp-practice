#include<bits/stdc++.h>
using namepspace std;
vector <int> twosum(vector <int> &nums , int target)
{
    
}

int main()
{
    vector<int> nums; 

    int n;
    cout<<"Enter the size of the array";
    cin>>n; 

    for(int i = 0 ; i < n ; i++)
    {
        int val;
        cin>>val;
        nums.push_back(val);
    }

    int target; 
    cout<<"Target = "; 
    cin>>target;
}